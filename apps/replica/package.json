{
  "name": "replica",
  "version": "1.0.0",
  "private": true,
  "description": "Java RAFT replica service",
  "scripts": {
    "build": "mvn -f pom.xml -DskipTests package",
    "run": "java -jar target/replica-1.0.0.jar"
  }
}
