{
  "name": "gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Java gateway service",
  "scripts": {
    "build": "mvn -f pom.xml -DskipTests package",
    "run": "java -jar target/gateway-1.0.0.jar"
  }
}
